server:
  port: 8080

spring:
  application:
    name: cabinate-api
  data:
    mongodb:
      # Format: mongodb://[username:password@]host[:port]/[database][?options]
      uri: mongodb://admin:password123@localhost:27017/cabinate?authSource=admin
      auto-index-creation: true
